--- "/mnt/user-data/uploads/index1_\344\277\256\346\255\243\347\211\210.html"	2026-09-22 05:39:01.181782000 +0000
+++ base.html	2026-09-22 05:48:10.560503288 +0000
@@ -1,4 +1,4 @@
-<!DOCTYPE html><html lang="zh-Hant"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover"><meta name="theme-color" content="#0a1417"><title>天下無雙・放置版 v1.0.0（3.0副本／4槽連招）</title><style>
+<!DOCTYPE html><html lang="zh-Hant"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover"><meta name="theme-color" content="#0a1417"><title>天下無雙放置版</title><style>
 :root{
   color-scheme:dark;
   --bg:#081114;--bg2:#0e1d21;--panel:#111a20;--panel2:#17242b;--line:#6e5530;
@@ -468,14 +468,14 @@
 const LEGACY_KEYS=['txws_idle_v530','txws_idle_v520','txws_idle_v510','txws_idle_v500','txws_shentong_single_v400','txws_shentong_single_v302','txws_xianlu_fusion_v1'];
 const TICK=180;
 const DEFAULT_AUTO={enabled:true,mode:'balanced',elementPref:'auto',autoSkill:true,autoPotion:true,hpThreshold:.45,mpThreshold:.25,autoAdvance:true,autoDungeon:true,autoBoss:true,bossGate:true,stopOnDeath:false,offline:true,stats:{kills:0,bossKills:0,drops:0,skillDrops:0,stones:0,exp:0,miss:0,crit:0}};
-function freshState(){return {version:GAME_VERSION,classDataVersion:'txws-official-1',createdAt:Date.now(),lastSave:Date.now(),offlineAt:Date.now(),offlineSummary:null,name:'少俠',selected:false,map:'taohua',log:[],inventory:{},inventoryInstances:{},equipment:{weapon:null,helmet:null,armor:null,bracer:null,belt:null,shoes:null,necklace:null,ring:null,artifact:null,talisman:null},skills:{equipped:[null,null,null,null],learned:{},cooldowns:{}},auto:JSON.parse(JSON.stringify(DEFAULT_AUTO)),forging:{},bossStatus:{},dungeons:{},quests:{claimed:{}},pets:{owned:['靈狐'],equipped:'靈狐'},talents:{points:0,狠:0,快:0,穩:0,智:0,準:0,metal:0,wood:0,water:0,fire:0,earth:0},inventoryQuality:{},player:{classKey:null,level:1,exp:0,spiritStone:500,attr:{狠:0,快:0,穩:0,智:0,準:0},preferredElement:'metal',stats:{}},setting:{lastPanel:'home',screenMode:'auto',comboLoop:true}}}
+function freshState(){return {version:GAME_VERSION,classDataVersion:'txws-official-1',createdAt:Date.now(),lastSave:Date.now(),offlineAt:Date.now(),offlineSummary:null,name:'少俠',selected:false,map:'taohua',log:[],inventory:{},inventoryInstances:{},equipment:{weapon:null,helmet:null,armor:null,bracer:null,belt:null,shoes:null,necklace:null,ring:null,artifact:null,talisman:null},skills:{equipped:[null,null,null,null],learned:{},cooldowns:{}},auto:JSON.parse(JSON.stringify(DEFAULT_AUTO)),forging:{},bossStatus:{},dungeons:{},quests:{claimed:{}},pets:{owned:['靈狐'],equipped:'靈狐'},talents:{points:0,elementPoints:0,狠:0,快:0,穩:0,智:0,準:0,metal:0,wood:0,water:0,fire:0,earth:0},inventoryQuality:{},player:{classKey:null,level:1,exp:0,spiritStone:500,attr:{狠:0,快:0,穩:0,智:0,準:0},preferredElement:'metal',stats:{}},setting:{lastPanel:'home',screenMode:'auto',comboLoop:true}}}
 let G=freshState(),battle=null,activePanel='home',mainLoop=null,nextSpawnAt=Date.now()+900,huntLog=[],pendingBoss=null,lastSecond=0;
 const $=id=>document.getElementById(id);const fmt=n=>Math.floor(Number(n)||0).toLocaleString('zh-TW');const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
 function toast(t){const d=document.createElement('div');d.className='toast';d.textContent=t;$('toastHost').appendChild(d);setTimeout(()=>d.remove(),2400)}
 function log(t,cls='lg-sys'){const s=new Date().toLocaleTimeString('zh-TW',{hour12:false})+' '+t;G.log.unshift(s);if(G.log.length>150)G.log.length=150;hlog(t,cls)}
 function hlog(t,cls='lg-sys'){huntLog.unshift({t,cls});if(huntLog.length>80)huntLog.length=80;paintLog()}
 function deepMerge(base,src){if(!src||typeof src!=='object')return base;for(const k of Object.keys(src)){if(src[k]&&typeof src[k]==='object'&&!Array.isArray(src[k])&&base[k]&&typeof base[k]==='object')deepMerge(base[k],src[k]);else base[k]=src[k]}return base}
-function normalize(){G=deepMerge(freshState(),G);G.version=GAME_VERSION;if(G.classDataVersion!=='txws-official-1'){G.selected=false;G.player.classKey=null;G.player.attr={狠:0,快:0,穩:0,智:0,準:0};G.classDataVersion='txws-official-1';}G.player.attr=Object.assign({狠:0,快:0,穩:0,智:0,準:0},G.player.attr||{});G.quests=G.quests||{claimed:{}};G.quests.claimed=G.quests.claimed||{};G.pets=G.pets||{owned:['靈狐'],equipped:'靈狐'};G.pets.owned=Array.isArray(G.pets.owned)&&G.pets.owned.length?G.pets.owned:['靈狐'];if(!PET_DB[G.pets.equipped]||!G.pets.owned.includes(G.pets.equipped))G.pets.equipped=G.pets.owned[0]||'靈狐';G.talents=Object.assign({points:0,狠:0,快:0,穩:0,智:0,準:0,metal:0,wood:0,water:0,fire:0,earth:0},G.talents||{});G.inventoryQuality=G.inventoryQuality||{};G.inventoryInstances=G.inventoryInstances||{};if(!CLASSES[G.player.classKey]){G.selected=false;G.player.classKey=null}if(['home','inventory','skill','shop','forge'].includes(G.setting?.lastPanel))G.setting.lastPanel='hunt';G.player.level=clamp(Number(G.player.level)||1,1,MAX_LEVEL_30);G.player.exp=Math.max(0,Number(G.player.exp)||0);G.player.spiritStone=Math.max(0,Number(G.player.spiritStone)||0);for(const slot of SLOT_ORDER)if(!(slot in G.equipment))G.equipment[slot]=null;for(const k of ['kills','bossKills','drops','skillDrops','stones','exp','miss','crit','hpUsed','mpUsed','deaths'])G.auto.stats[k]=Number(G.auto.stats[k])||0;if(!FIVE[G.player.preferredElement])G.player.preferredElement='metal';if(!maps.find(m=>m.id===G.map))G.map='taohua';if(G.auto.elementPref==='auto'){}else if(!FIVE[G.auto.elementPref])G.auto.elementPref='auto';G.setting=G.setting||{lastPanel:'home',screenMode:'auto',comboLoop:true};G.setting.comboLoop=G.setting.comboLoop!==false;G.skills=G.skills||{equipped:[null,null,null,null],learned:{},cooldowns:{}};G.skills.equipped=(Array.isArray(G.skills.equipped)?G.skills.equipped:[]).slice(0,4);while(G.skills.equipped.length<4)G.skills.equipped.push(null);G.dungeons=G.dungeons||{};G.dungeons.five_elements_tower=G.dungeons.five_elements_tower||{floor:1,best:0};G.dungeons.trial_temple=G.dungeons.trial_temple||{best:0,runs:0};G.dungeons.maze_treasure=G.dungeons.maze_treasure||{runs:0,best:0,path:[]};G.dungeons.soul_instance=G.dungeons.soul_instance||{lastDay:'',clears:0};G.dungeons.monster_valley=G.dungeons.monster_valley||{runs:0,materials:0};G.dungeons.sand_demon_lair=G.dungeons.sand_demon_lair||{runs:0,best:0}}
+function normalize(){G=deepMerge(freshState(),G);G.version=GAME_VERSION;if(G.classDataVersion!=='txws-official-1'){G.selected=false;G.player.classKey=null;G.player.attr={狠:0,快:0,穩:0,智:0,準:0};G.classDataVersion='txws-official-1';}G.player.attr=Object.assign({狠:0,快:0,穩:0,智:0,準:0},G.player.attr||{});G.quests=G.quests||{claimed:{}};G.quests.claimed=G.quests.claimed||{};G.pets=G.pets||{owned:['靈狐'],equipped:'靈狐'};G.pets.owned=Array.isArray(G.pets.owned)&&G.pets.owned.length?G.pets.owned:['靈狐'];if(!PET_DB[G.pets.equipped]||!G.pets.owned.includes(G.pets.equipped))G.pets.equipped=G.pets.owned[0]||'靈狐';G.talents=Object.assign({points:0,elementPoints:0,狠:0,快:0,穩:0,智:0,準:0,metal:0,wood:0,water:0,fire:0,earth:0},G.talents||{});G.inventoryQuality=G.inventoryQuality||{};G.inventoryInstances=G.inventoryInstances||{};if(!CLASSES[G.player.classKey]){G.selected=false;G.player.classKey=null}if(['home','inventory','skill','shop','forge'].includes(G.setting?.lastPanel))G.setting.lastPanel='hunt';G.player.level=clamp(Number(G.player.level)||1,1,MAX_LEVEL_30);G.player.exp=Math.max(0,Number(G.player.exp)||0);G.player.spiritStone=Math.max(0,Number(G.player.spiritStone)||0);for(const slot of SLOT_ORDER)if(!(slot in G.equipment))G.equipment[slot]=null;for(const k of ['kills','bossKills','drops','skillDrops','stones','exp','miss','crit','hpUsed','mpUsed','deaths'])G.auto.stats[k]=Number(G.auto.stats[k])||0;if(!FIVE[G.player.preferredElement])G.player.preferredElement='metal';if(!maps.find(m=>m.id===G.map))G.map='taohua';if(G.auto.elementPref==='auto'){}else if(!FIVE[G.auto.elementPref])G.auto.elementPref='auto';G.setting=G.setting||{lastPanel:'home',screenMode:'auto',comboLoop:true};G.setting.comboLoop=G.setting.comboLoop!==false;G.skills=G.skills||{equipped:[null,null,null,null],learned:{},cooldowns:{}};G.skills.equipped=(Array.isArray(G.skills.equipped)?G.skills.equipped:[]).slice(0,4);while(G.skills.equipped.length<4)G.skills.equipped.push(null);G.dungeons=G.dungeons||{};G.dungeons.five_elements_tower=G.dungeons.five_elements_tower||{floor:1,best:0};G.dungeons.trial_temple=G.dungeons.trial_temple||{best:0,runs:0};G.dungeons.maze_treasure=G.dungeons.maze_treasure||{runs:0,best:0,path:[]};G.dungeons.soul_instance=G.dungeons.soul_instance||{lastDay:'',clears:0};G.dungeons.monster_valley=G.dungeons.monster_valley||{runs:0,materials:0};G.dungeons.sand_demon_lair=G.dungeons.sand_demon_lair||{runs:0,best:0}}
 function save(quiet=false){G.lastSave=Date.now();G.offlineAt=Date.now();try{localStorage.setItem(SAVE_KEY,JSON.stringify(G));if(!quiet)toast('存檔完成')}catch(e){if(!quiet)toast('無法寫入存檔')}}
 function load(){let raw=null;try{raw=localStorage.getItem(SAVE_KEY)}catch(e){}if(raw){try{G=JSON.parse(raw)}catch(e){G=freshState()}}else{for(const key of LEGACY_KEYS){try{const o=localStorage.getItem(key);if(o){G=JSON.parse(o);break}}catch(e){}}}normalize();applyOffline();ensureStats();refreshMarket()}
 function applyOffline(){const now=Date.now(),last=Number(G.offlineAt)||now;const sec=clamp((now-last)/1000,0,86400);if(sec>=5&&G.selected){const beforeInv={...(G.inventory||{})};const rate=G.auto.enabled?.95:.22;const kills=Math.floor(sec/6*rate);const exp=Math.floor(kills*(5+G.player.level*.35));const stones=Math.floor(kills*(2+G.player.level*.06));G.player.exp+=exp;G.player.spiritStone+=stones;G.auto.stats.kills+=kills;G.auto.stats.exp+=exp;G.auto.stats.stones+=stones;G.player.stats.hp=1;ensureStats();G.player.stats.hp=G.player.stats.maxHp;G.player.stats.mp=G.player.stats.maxMp;for(let i=0;i<Math.floor(kills*.25);i++)offlineDrop();levelUp();const itemDrops=Object.keys(G.inventory||{}).map(name=>({name,qty:(G.inventory[name]||0)-(beforeInv[name]||0)})).filter(x=>x.qty>0);G.offlineSummary={seconds:sec,kills,exp,stones,itemDrops,at:now};G.log.unshift(`離線 ${Math.floor(sec/3600)} 小時 ${Math.floor((sec%3600)/60)} 分：獲得經驗 ${fmt(exp)}、靈石 ${fmt(stones)}、掉落 ${itemDrops.map(x=>x.name+'×'+x.qty).join('、')||'無'}`)}G.offlineAt=now}
@@ -483,7 +483,7 @@
 function refreshMarket(){const day=new Date().toISOString().slice(0,10);if(G.marketDay===day&&Array.isArray(G.market))return;G.marketDay=day;G.market=MARKET.map(x=>({name:x[0],price:Math.round(x[1]*(.85+Math.random()*.3)),type:x[2],key:x[3]}))}
 function classData(){return CLASSES[G.player.classKey]||null}
 function expNeed(){return Math.floor(60*Math.pow(G.player.level,1.8))}
-function levelUp(){while(G.player.exp>=expNeed()&&G.player.level<MAX_LEVEL_30){G.player.exp-=expNeed();G.player.level++;const c=classData();for(const k of Object.keys(c.growth))G.player.attr[k]+=c.growth[k];G.talents.points=(G.talents.points||0)+1;log(`等級提升至 Lv.${G.player.level}，天賦點 +1`,'lg-drop');toast(`升級 Lv.${G.player.level}`);ensureStats()}}
+function levelUp(){while(G.player.exp>=expNeed()&&G.player.level<MAX_LEVEL_30){G.player.exp-=expNeed();G.player.level++;const c=classData();for(const k of Object.keys(c.growth))G.player.attr[k]+=c.growth[k];G.talents.points=(G.talents.points||0)+1;let msg=`等級提升至 Lv.${G.player.level}，天賦點 +1`;if(G.player.level%5===0){G.talents.elementPoints=(G.talents.elementPoints||0)+1;msg+='，五行天賦點 +1'}log(msg,'lg-drop');toast(`升級 Lv.${G.player.level}`);ensureStats()}}
 function chooseClass(key){const c=CLASSES[key];if(!c)return;G.player.classKey=key;G.selected=true;G.classDataVersion='txws-official-1';G.player.attr={...c.attrs};G.player.level=1;G.player.exp=0;G.name=($('startName')?.value||'少俠').slice(0,16)||'少俠';G.player.name=G.name;CLASS_SKILLS[key].forEach(n=>learnSkill(n,true));G.skills.equipped=CLASS_SKILLS[key].map(n=>SKILLS[n]?.id).filter(Boolean).slice(0,4);const starter={劍宗:['精鐵劍','青布帽','厚皮甲','青鐵護腕','皮革腰帶','青布靴','青玉項鍊','青玉戒','青玉法寶','平安護符'],戟門:['精鐵戟','青布帽','厚皮甲','青鐵護腕','皮革腰帶','青布靴','青玉項鍊','青玉戒','青玉法寶','平安護符'],詭流:['精鐵鏜','青布帽','厚皮甲','青鐵護腕','皮革腰帶','青布靴','青玉項鍊','青玉戒','青玉法寶','平安護符'],幻道:['桃木杖','青布帽','厚皮甲','青鐵護腕','皮革腰帶','青布靴','青玉項鍊','青玉戒','青玉法寶','平安護符']}[key]||[];starter.forEach(n=>G.inventory[n]=(G.inventory[n]||0)+1);save(true);$('createScreen')?.remove();toast(`建立 ${key} 角色完成`);log(`${G.name} 加入 ${key}，職業取向：${c.ai.focus}`,'lg-drop');startAuto();goPanel('character')}
 function learnSkill(name,silent=false){const sk=SKILLS[name];if(!sk)return false;if(!G.skills.learned[sk.id]){G.skills.learned[sk.id]={level:1};if(!silent)toast('領悟神通：'+name);return true}return false}
 function skillByName(n){return SKILLS[n]||Object.values(SKILLS).find(s=>s.name===n)}
@@ -491,7 +491,7 @@
 function buyPet(name){const p=PET_DB[name];if(!p)return;if(G.pets.owned.includes(name))return equipPet(name);if(G.player.spiritStone<p.cost)return toast('靈石不足');G.player.spiritStone-=p.cost;G.pets.owned.push(name);G.pets.equipped=name;toast(`獲得靈寵：${name}`);ensureStats();save(true);renderAll()}
 function equipPet(name){if(!PET_DB[name]||!G.pets.owned.includes(name))return;G.pets.equipped=name;ensureStats();toast(`出戰靈寵：${name}`);renderAll()}
 function spendTalent(k,amount=1){amount=clamp(Number(amount)||1,1,10);if(!(k in G.talents)||!G.talents.points)return toast('沒有天賦點');amount=Math.min(amount,G.talents.points);G.talents.points-=amount;G.talents[k]+=amount;ensureStats();toast(`天賦 ${k} +${amount}`);save(true);renderAll()}
-function spendElementTalent(k,amount=1){amount=clamp(Number(amount)||1,1,10);if(!['metal','wood','water','fire','earth'].includes(k)||!G.talents.points)return toast('沒有天賦點');amount=Math.min(amount,G.talents.points);G.talents.points-=amount;G.talents[k]=(G.talents[k]||0)+amount;G.player.preferredElement=k;ensureStats();toast(`五行天賦 ${FIVE[k].name} +${amount}`);save(true);renderAll()}
+function spendElementTalent(k,amount=1){amount=clamp(Number(amount)||1,1,10);if(!['metal','wood','water','fire','earth'].includes(k)||!G.talents.elementPoints)return toast('沒有五行天賦點');amount=Math.min(amount,G.talents.elementPoints);G.talents.elementPoints-=amount;G.talents[k]=(G.talents[k]||0)+amount;G.player.preferredElement=k;ensureStats();toast(`五行天賦 ${FIVE[k].name} +${amount}`);save(true);renderAll()}
 function questProgress(q){return Math.min(q.target,Number(G.auto.stats[q.type]||0))}
 function claimQuest(id){const q=QUESTS.find(x=>x.id===id);if(!q||G.quests.claimed[id])return;if(questProgress(q)<q.target)return toast('任務尚未完成');G.quests.claimed[id]=true;G.player.spiritStone+=q.reward;toast(`任務完成：${q.name}，靈石 +${fmt(q.reward)}`);save(true);renderAll()}
 
@@ -570,7 +570,7 @@
 function bossRemain(id){const st=G.bossStatus[id]||{};return Math.max(0,Math.ceil(((st.respawnAt||0)-Date.now())/1000))}
 
 /* ---------- 煉器殿 8 大功能 ---------- */
-function forgeEnhance(slot){const it=G.equipment[slot];if(!it)return toast('該部位沒有裝備');if((it.enhance||0)>=15)return toast('已達 +15');const lv=(it.enhance||0)+1,cost=30000; if(G.player.spiritStone<cost)return toast('靈石不足');G.player.spiritStone-=cost;it.enhance=lv;it.baseAtk=Math.round((it.baseAtk||0)*1.06+2);it.baseDef=Math.round((it.baseDef||0)*1.06+1);if(it.hp)it.hp=Math.round(it.hp*1.045);toast(`${it.name} 強化 +${lv}`);ensureStats();renderAll()}
+function forgeEnhance(slot){const it=G.equipment[slot];if(!it)return toast('該部位沒有裝備');if((it.enhance||0)>=15)return toast('已達 +15');const lv=(it.enhance||0)+1,cost=30000*lv; if(G.player.spiritStone<cost)return toast('靈石不足');G.player.spiritStone-=cost;if(Math.random()<.5){it.enhance=lv;it.baseAtk=Math.round((it.baseAtk||0)*1.06+2);it.baseDef=Math.round((it.baseDef||0)*1.06+1);if(it.hp)it.hp=Math.round(it.hp*1.045);toast(`${it.name} 強化 +${lv}`)}else{toast(`${it.name} 強化失敗`)}ensureStats();renderAll()}
 function forgeRefine(slot){const it=G.equipment[slot];if(!it)return toast('該部位沒有裝備');if((G.inventory['精煉石']||0)<1)return toast('需要精煉石');if((it.refine||0)>=8)return toast('精煉已達上限');G.inventory['精煉石']--;it.refine=(it.refine||0)+1;toast(`${it.name} 精煉 ${it.refine}%`);ensureStats();renderAll()}
 function forgeSocket(slot){const it=G.equipment[slot];if(!it)return toast('該部位沒有裝備');if((it.sockets||0)>=3)return toast('最多 3 孔');if((G.inventory['打孔石']||0)<1)return toast('需要打孔石');G.inventory['打孔石']--;it.sockets=(it.sockets||0)+1;it.gems=it.gems||[];toast(`${it.name} 開孔至 ${it.sockets} 孔`);renderAll()}
 const GEM_KEYS=['攻擊靈石','防禦靈石','氣血靈石','金靈石','木靈石','水靈石','火靈石','土靈石'];
@@ -578,7 +578,7 @@
 function forgeReroll(slot){const it=G.equipment[slot];if(!it)return toast('該部位沒有裝備');if((G.inventory['玄鐵']||0)<3)return toast('需要玄鐵 ×3');G.inventory['玄鐵']-=3;it.affix=randomAffix(it.level);toast(`${it.name} 洗煉為 ${it.affix.key}+${it.affix.value}`);ensureStats();renderAll()}
 function dismantleInventory(name){const qty=G.inventory[name]||0;if(!qty||!EQUIP_DB[name])return toast('沒有可分解裝備');G.inventory[name]--;if(G.inventoryInstances?.[name]?.length)G.inventoryInstances[name].pop();const it=EQUIP_DB[name];const iron=Math.max(1,Math.floor(it.level/8));const stone=Math.max(1,Math.floor(it.level/15));G.inventory['玄鐵']=(G.inventory['玄鐵']||0)+iron;G.player.spiritStone+=stone;toast(`${name} 分解：玄鐵×${iron}、靈石×${stone}`);renderAll()}
 function synthesize(name){const d=EQUIP_DB[name];if(!d)return;const q=G.inventoryQuality?.[name]||d.quality;const same=EQUIP_NAMES.filter(n=>EQUIP_DB[n].slot===d.slot&&(G.inventoryQuality?.[n]||EQUIP_DB[n].quality)===q).filter(n=>(G.inventory[n]||0)>0);const selected=[];for(const n of same){while((G.inventory[n]||0)>0&&selected.length<3){selected.push(n);G.inventory[n]--;if(selected.length===3)break}}if(selected.length<3){selected.forEach(n=>G.inventory[n]=(G.inventory[n]||0)+1);return toast('需要 3 件同品質同部位裝備')}const order=['white','green','blue','purple','gold'],next=order[Math.min(order.length-1,order.indexOf(q)+1)];G.inventoryQuality[name]=next;G.inventory[name]=(G.inventory[name]||0)+1;toast(next==='gold'?`合成成功：${name} 已達天品`:`合成成功：${name} 品質提升至 ${QUALITY[next]}`);renderAll()}
-function enhanceEquippedAll(){let n=0;for(const s of SLOT_ORDER){const it=G.equipment[s];if(it?.enhance<15){const before=it.enhance;for(let i=before;i<15&&G.player.spiritStone>=80*(i+1);i++)forgeEnhance(s);n++}}if(n)toast('已強化可用裝備')}
+function enhanceEquippedAll(){let n=0;for(const s of SLOT_ORDER){const it=G.equipment[s];if(it?.enhance<15){const before=it.enhance;for(let i=before;i<15&&G.player.spiritStone>=30000*(i+1);i++)forgeEnhance(s);n++}}if(n)toast('已強化可用裝備')}
 
 function buyShopItem(kind,key,name){const item=G.market?.[key];const row=MARKET[key];if(!row)return;const cost=item?.price||row[1];if(G.player.spiritStone<cost)return toast('靈石不足');G.player.spiritStone-=cost;if(row[2]==='potion'||row[2]==='item')G.inventory[row[3]]=(G.inventory[row[3]]||0)+1;else G.inventory[row[3]]=(G.inventory[row[3]]||0)+1;toast('購入 '+row[0]);renderAll()}
 function buyEquip(name){const d=EQUIP_DB[name];if(!d)return;if(d.level>50)return toast('Lv.51以上裝備改由打怪掉落');if(G.player.level<d.level)return toast(`需要 Lv.${d.level}`);if(G.player.level<d.level)return toast(`需要 Lv.${d.level}`);const cost=Math.max(100,Math.round(Math.pow(d.level,1.58)*12));if(G.player.spiritStone<cost)return toast('靈石不足');G.player.spiritStone-=cost;G.inventory[name]=(G.inventory[name]||0)+1;toast(`購入 ${name}`);renderAll()}
@@ -593,7 +593,7 @@
 function goPanel(p){activePanel=p;G.setting.lastPanel=p;renderAll()}
 function renderSide(){const sideItems=[['home','回','主城'],['home','門','江湖'],['hunt','⚔','獵場'],['inventory','袋','背包'],['skill','符','神通'],['shop','市','店舖'],['forge','爐','煉器'],['boss','王','Boss'],['system','⚙','設定']];const dockItems=[['character','人','角色'],['hunt','⚔','獵場'],['inventory','袋','背包'],['skill','符','神通'],['shop','市','店舖'],['forge','爐','煉器'],['boss','王','Boss'],['system','⚙','設定']];$('side').innerHTML=sideItems.map(([id,ic,n])=>`<button class="nav ${activePanel===id?'active':''}" data-panel="${id}" data-return-home="${id==='home'&&n==='主城'?'1':''}"><i>${ic}</i>${n}</button>`).join('');$('dock').innerHTML=dockItems.map(([id,ic,n])=>`<button class="${activePanel===id?'active':''}" data-panel="${id}"><b>${ic}</b>${n}</button>`).join('');document.querySelectorAll('[data-panel]').forEach(b=>b.onclick=()=>{if(b.dataset.returnHome==='1'){returnHome();return}activePanel=b.dataset.panel;G.setting.lastPanel=activePanel;renderAll()})}
 function classAiName(slot){const c=classData()||CLASSES.劍宗,L=G.player.level;const rows=EQUIP_NAMES.map(name=>({name,d:EQUIP_DB[name]})).filter(x=>x.d.slot===slot&&x.d.level<=Math.max(1,L+5)).sort((a,b)=>equipScore(b.d)-equipScore(a.d)||Math.abs(a.d.level-L)-Math.abs(b.d.level-L));return rows[0]?.name||'尚無合適裝備'}
-function renderCharacter(){const p=G.player,s=p.stats,c=classData();if(!c)return `<div class="card"><h2 class="title">角色</h2><p class="desc">尚未建立角色。</p><button class="bigbtn" onclick="openClassScreen()">建立天下無雙角色</button></div>`;const maxAttr=Math.max(1,...Object.values(s.attr||{}));const attrs=Object.entries(s.attr||{}).map(([k,v])=>`<div class="stat" style="display:block"><div class="line"><b>${k}</b><em>${v.toFixed(1)}</em></div><div class="attr-bar"><i style="width:${Math.min(100,(v/maxAttr)*100)}%"></i></div></div>`).join('');const eqHtml=SLOT_ORDER.map(slot=>{const it=G.equipment[slot],rec=classAiName(slot);return `<div class="role-eq ${it?'':'empty'}"><b>${SLOT_NAMES[slot]}</b><strong>${it?it.name:'未裝備'}</strong><span>${it?`${QUALITY[it.quality]||'凡品'} · +${it.enhance||0} · ${Object.entries(it.attr||{}).filter(([,v])=>v).map(([k,v])=>k+'+'+v).join('、')||'基礎屬性'}`:`手動選擇裝備`}</span></div>`}).join('');return `<div class="card"><div class="role-head"><div class="role-avatar">${c.icon}</div><div><div class="role-title">${G.name||'少俠'}</div><div class="role-sub">${G.player.classKey} · ${c.weapon}系武器 · Lv.${p.level}</div><p class="desc" style="margin-top:4px">${c.desc}</p></div></div><div class="row" style="margin-top:8px"><span class="badge">官方初始：狠${c.attrs.狠} 準${c.attrs.準} 穩${c.attrs.穩} 快${c.attrs.快} 智${c.attrs.智}</span><span class="badge">AI取向：${c.ai.focus}</span></div></div><div class="card"><h3 class="title">人物屬性</h3><div class="statgrid">${attrs}</div><div class="statgrid"><div class="stat">攻擊<em>${fmt(s.atk)}</em></div><div class="stat">防禦<em>${fmt(s.def)}</em></div><div class="stat">氣血<em>${fmt(s.maxHp)}</em></div><div class="stat">法力<em>${fmt(s.maxMp)}</em></div><div class="stat">命中<em>${(s.accuracy*100).toFixed(1)}%</em></div><div class="stat">閃避<em>${(s.evasion*100).toFixed(1)}%</em></div><div class="stat">暴擊<em>${(s.critRate*100).toFixed(1)}%</em></div><div class="stat">速度<em>${s.speed.toFixed(1)}</em></div></div><p class="footer-note">官方人物屬性意義：狠＝招式威力、準＝命中、穩＝HP、快＝閃避、智＝配合大招的真氣需求；本放置版再把這五項直接用於 AI 配裝。</p></div><div class="card"><div class="line"><h3 class="title" style="margin:0">角色裝備</h3><button class="smallbtn hot" onclick="goPanel('shop')">找裝備</button></div><div class="role-eqgrid">${eqHtml}</div></div><div class="card"><div class="line"><h3 class="title" style="margin:0">五屬天賦</h3><span class="badge">可分配：${G.talents.points||0}</span></div><div class="row" style="margin:6px 0"><span class="desc">每次配點</span><select id="talentAmount" class="smallbtn" style="padding:5px 8px"><option value="1">1 點</option><option value="10">10 點</option></select></div><p class="desc">天賦點由自動升級取得；可選 1 點／10 點後再投入屬性。</p><div class="fx-grid">${['狠','快','穩','智','準'].map(k=>`<div class="fx"><div class="line"><b>${k}</b><span>${G.talents[k]||0}</span></div><button class="smallbtn" style="margin-top:5px" onclick="spendTalent('${k}',Number(document.getElementById('talentAmount')?.value||1))" ${(G.talents.points||0)<=0?'disabled':''}>投入</button></div>`).join('')}</div></div><div class="card"><div class="line"><h3 class="title" style="margin:0">五行天賦</h3><span class="badge">可分配：${G.talents.points||0}</span></div><p class="desc">金、木、水、火、土五行天賦點可自由分配；使用上方 1 點／10 點選單。</p><div class="fx-grid">${['metal','wood','water','fire','earth'].map(k=>`<div class="fx"><div class="line"><b>${FIVE[k].name}</b><span>${G.talents[k]||0}</span></div><button class="smallbtn" style="margin-top:5px" onclick="spendElementTalent('${k}',Number(document.getElementById('talentAmount')?.value||1))" ${(G.talents.points||0)<=0?'disabled':''}>投入</button></div>`).join('')}</div></div><div class="card"><h3 class="title">手動配裝</h3><div class="role-ai"><b>${G.player.classKey}</b>：${c.ai.note}<br>裝備不再自動穿戴；請從背包手動選擇武器與防具，系統提供數值對比後由玩家決定。</div><div class="row" style="margin-top:8px"><button class="smallbtn hot" onclick="goPanel('inventory')">開啟背包裝備</button></div></div>`}
+function renderCharacter(){const p=G.player,s=p.stats,c=classData();if(!c)return `<div class="card"><h2 class="title">角色</h2><p class="desc">尚未建立角色。</p><button class="bigbtn" onclick="openClassScreen()">建立天下無雙角色</button></div>`;const maxAttr=Math.max(1,...Object.values(s.attr||{}));const attrs=Object.entries(s.attr||{}).map(([k,v])=>`<div class="stat" style="display:block"><div class="line"><b>${k}</b><em>${v.toFixed(1)}</em></div><div class="attr-bar"><i style="width:${Math.min(100,(v/maxAttr)*100)}%"></i></div></div>`).join('');const eqHtml=SLOT_ORDER.map(slot=>{const it=G.equipment[slot],rec=classAiName(slot);return `<div class="role-eq ${it?'':'empty'}"><b>${SLOT_NAMES[slot]}</b><strong>${it?it.name:'未裝備'}</strong><span>${it?`${QUALITY[it.quality]||'凡品'} · +${it.enhance||0} · ${Object.entries(it.attr||{}).filter(([,v])=>v).map(([k,v])=>k+'+'+v).join('、')||'基礎屬性'}`:`手動選擇裝備`}</span></div>`}).join('');return `<div class="card"><div class="role-head"><div class="role-avatar">${c.icon}</div><div><div class="role-title">${G.name||'少俠'}</div><div class="role-sub">${G.player.classKey} · ${c.weapon}系武器 · Lv.${p.level}</div><p class="desc" style="margin-top:4px">${c.desc}</p></div></div><div class="row" style="margin-top:8px"><span class="badge">官方初始：狠${c.attrs.狠} 準${c.attrs.準} 穩${c.attrs.穩} 快${c.attrs.快} 智${c.attrs.智}</span><span class="badge">AI取向：${c.ai.focus}</span></div></div><div class="card"><h3 class="title">人物屬性</h3><div class="statgrid">${attrs}</div><div class="statgrid"><div class="stat">攻擊<em>${fmt(s.atk)}</em></div><div class="stat">防禦<em>${fmt(s.def)}</em></div><div class="stat">氣血<em>${fmt(s.maxHp)}</em></div><div class="stat">法力<em>${fmt(s.maxMp)}</em></div><div class="stat">命中<em>${(s.accuracy*100).toFixed(1)}%</em></div><div class="stat">閃避<em>${(s.evasion*100).toFixed(1)}%</em></div><div class="stat">暴擊<em>${(s.critRate*100).toFixed(1)}%</em></div><div class="stat">速度<em>${s.speed.toFixed(1)}</em></div></div><p class="footer-note">官方人物屬性意義：狠＝招式威力、準＝命中、穩＝HP、快＝閃避、智＝配合大招的真氣需求；本放置版再把這五項直接用於 AI 配裝。</p></div><div class="card"><div class="line"><h3 class="title" style="margin:0">角色裝備</h3><button class="smallbtn hot" onclick="goPanel('shop')">找裝備</button></div><div class="role-eqgrid">${eqHtml}</div></div><div class="card"><div class="line"><h3 class="title" style="margin:0">五屬天賦</h3><span class="badge">可分配：${G.talents.points||0}</span></div><div class="row" style="margin:6px 0"><span class="desc">每次配點</span><select id="talentAmount" class="smallbtn" style="padding:5px 8px"><option value="1">1 點</option><option value="10">10 點</option></select></div><p class="desc">天賦點由自動升級取得；可選 1 點／10 點後再投入屬性。</p><div class="fx-grid">${['狠','快','穩','智','準'].map(k=>`<div class="fx"><div class="line"><b>${k}</b><span>${G.talents[k]||0}</span></div><button class="smallbtn" style="margin-top:5px" onclick="spendTalent('${k}',Number(document.getElementById('talentAmount')?.value||1))" ${(G.talents.points||0)<=0?'disabled':''}>投入</button></div>`).join('')}</div></div><div class="card"><div class="line"><h3 class="title" style="margin:0">五行天賦</h3><span class="badge">可分配：${G.talents.elementPoints||0}</span></div><p class="desc">金、木、水、火、土五行天賦點可自由分配；每 5 級取得 1 點；使用上方 1 點／10 點選單。</p><div class="fx-grid">${['metal','wood','water','fire','earth'].map(k=>`<div class="fx"><div class="line"><b>${FIVE[k].name}</b><span>${G.talents[k]||0}</span></div><button class="smallbtn" style="margin-top:5px" onclick="spendElementTalent('${k}',Number(document.getElementById('talentAmount')?.value||1))" ${(G.talents.elementPoints||0)<=0?'disabled':''}>投入</button></div>`).join('')}</div></div><div class="card"><h3 class="title">手動配裝</h3><div class="role-ai"><b>${G.player.classKey}</b>：${c.ai.note}<br>裝備不再自動穿戴；請從背包手動選擇武器與防具，系統提供數值對比後由玩家決定。</div><div class="row" style="margin-top:8px"><button class="smallbtn hot" onclick="goPanel('inventory')">開啟背包裝備</button></div></div>`}
 function renderHome(){const p=G.player,s=p.stats,c=classData(),pt=petData();const quests=QUESTS.map(q=>{const pr=questProgress(q),done=!!G.quests.claimed[q.id];return `<div class="feature-list"><div><b>${q.name}</b> · ${pr}/${q.target} · ${q.desc} ${done?'✅':pr>=q.target?`<button class="smallbtn good" onclick="claimQuest('${q.id}')">領取 +${fmt(q.reward)}</button>`:''}</div></div>`}).join('');return `<div class="card"><h2 class="title">天下無雙・放置版</h2><p class="desc">${c?c.desc:'請建立角色'} 以「打怪—掉寶—配裝—煉器—升級」作為單機循環，掛機與離線收益自動運轉。</p><div class="statgrid"><div class="stat">門派<em>${G.player.classKey||'未選'}</em></div><div class="stat">等級<em>Lv.${p.level}</em></div><div class="stat">攻擊<em>${fmt(s.atk)}</em></div><div class="stat">防禦<em>${fmt(s.def)}</em></div><div class="stat">出手<em>${s.speed.toFixed(1)}</em></div><div class="stat">暴擊<em>${(s.critRate*100).toFixed(1)}%</em></div></div></div><div class="card"><h3 class="title">狠 · 快 · 穩 · 智 · 準</h3><div class="fx-grid">${Object.entries(s.attr||{}).map(([k,v])=>`<div class="fx"><b>${k}</b><span>${v.toFixed(1)}</span></div>`).join('')}</div><p class="footer-note">狠：招式威力；準：命中；穩：HP；快：閃避／攻速；智：MP。五行：金→水→木→火→土→金。克制 ×1.20，被克 ×0.85。</p></div><div class="card"><h3 class="title">離線收益（最多 24 小時）</h3><p class="desc">離開遊戲後依最後存檔時間模擬掛機收益，單次最多計算 24 小時；回來後一次結算。</p>${G.offlineSummary?`<div class="feature-list"><div><b>上次結算：</b>${Math.floor(G.offlineSummary.seconds/3600)} 小時 ${Math.floor(G.offlineSummary.seconds%3600/60)} 分 · 擊殺 ${fmt(G.offlineSummary.kills)} · 經驗 +${fmt(G.offlineSummary.exp)} · 靈石 +${fmt(G.offlineSummary.stones)}</div><div><b>掛機掉落：</b>${(G.offlineSummary.itemDrops||[]).map(x=>x.name+' ×'+x.qty).join('、')||'無'}</div></div>`:'<p class="footer-note">尚無離線收益紀錄。</p>'}</div><div class="card"><h3 class="title">自動循環</h3><div class="feature-list"><div><b>① 掛機：</b>自動尋怪、選神通、服藥、掉寶，裝備由玩家手動穿戴／卸下。</div><div><b>② 推圖：</b>達到等級後 AI 自動前進高階狩獵區。</div><div><b>③ 副本／Boss：</b>可手動挑戰，也可由 AI 自動進場。</div><div><b>④ 離線：</b>離開後模擬掛機收益，回來直接領取。</div></div><div class="row" style="margin-top:8px"><button class="smallbtn hot" onclick="toggleAuto()">${G.auto.enabled?'停止掛機':'開始掛機'}</button><button class="smallbtn" onclick="goPanel('character')">角色／裝備</button><button class="smallbtn" onclick="goPanel('shop')">裝備／靈寵</button><button class="smallbtn" onclick="goPanel('forge')">去煉器</button></div></div><div class="card"><h3 class="title">${pt.icon} 靈寵 · ${pt.name}</h3><p class="desc">${pt.desc} · 攻 ${pt.atk} · 防 ${pt.def} · HP ${pt.hp} · 快 ${pt.fast} · 暴擊 ${(pt.crit*100).toFixed(1)}%</p><button class="smallbtn" onclick="goPanel('shop')">前往靈寵苑切換</button></div><div class="card"><h3 class="title">天賦／任務</h3><p class="desc">未分配天賦點：${G.talents.points||0} · 升級取得 1 點。</p>${quests}</div>`}
 
 function renderWorld(){const m=currentMap();const pool=mapPool();return `<div class="hunt" id="huntBox"><div class="huntline"><span class="foe lv-easy" id="huntFoe">${G.auto.enabled?'搜尋對手…':'尚未開戰'}</span><span class="monster-meta" id="huntBuffs">${G.player.classKey||''}</span></div><div class="foebar"><i id="huntBarFill" style="width:0%"></i></div><div class="monster-meta" id="huntMeta" style="margin-top:5px">${m.name} · ${m.zone}</div><div class="tickers"><div class="ticker">擊殺<b id="tkKill">${fmt(G.auto.stats.kills)}</b></div><div class="ticker">靈石<b id="tkStone">${fmt(G.auto.stats.stones)}</b></div><div class="ticker">掉落<b id="tkDrop">${fmt(G.auto.stats.drops)}</b></div></div><div class="row" style="margin-top:8px"><button class="smallbtn hot" onclick="toggleAuto()">${G.auto.enabled?'停止掛機':'開始掛機'}</button><button class="smallbtn" onclick="openBattleScreen()">戰鬥畫面</button><button class="smallbtn danger" onclick="flee()">撤退</button></div><div class="battlelog" id="huntLog"></div></div><div class="card"><h2 class="title">${m.name}</h2><p class="desc">${m.desc}</p><div class="maplist">${pool.map(x=>{const d=monsterData(x.name,x.level);return `<div class="map"><div class="line"><strong class="mapname">${x.name}</strong><span class="badge">Lv.${x.level} · ${elementName(d.element)}</span></div><div class="monster-meta">種族 ${d.race||'獸'} · 攻 ${fmt(d.atk)} · 防 ${fmt(d.def)} · HP ${fmt(d.maxHp)}</div></div>`}).join('')}</div></div>`}
@@ -610,9 +610,9 @@
 function equipSkill(id){const idx=(G.skills.equipped||[]).indexOf(id);if(idx>=0){assignSkillSlot(id,-1);return}const slot=(G.skills.equipped||[]).findIndex(x=>!x);assignSkillSlot(id,slot<0?0:slot)}
 function renderShop(){const L=G.player.level;const all=EQUIP_NAMES.slice().filter(n=>EQUIP_DB[n].level<=50).sort((a,b)=>EQUIP_DB[a].level-EQUIP_DB[b].level);const weapons=all.filter(n=>EQUIP_DB[n].slot==='weapon');const armor=all.filter(n=>EQUIP_DB[n].slot!=='weapon');const equipCard=n=>{const d=EQUIP_DB[n],locked=L<d.level,cost=Math.max(100,Math.round(Math.pow(d.level,1.58)*12));return `<div class="shopitem"><div class="line"><strong class="quality-${d.quality}">${d.slot==='weapon'?'⚔':'🛡'} ${n}</strong><span class="price">${fmt(cost)}</span></div><div class="desc">Lv.${d.level} · ${SLOT_NAMES[d.slot]} · 五行 ${elementName(d.element)} · 攻 ${fmt(d.baseAtk)} · 防 ${fmt(d.baseDef)} · HP ${fmt(d.hp)}<br>${Object.entries(d.attr).filter(([,v])=>v).map(([k,v])=>k+'+'+v).join('、')||'基礎屬性'}<br><span class="muted">${d.source}</span></div><button class="smallbtn ${locked?'':'good'}" style="margin-top:5px" ${locked?'disabled':''} onclick="buyEquip('${n}')">${locked?'Lv.'+d.level+'解鎖':'購買'}</button></div>`};const starter=`<div class="shopgrid">${Object.values(BASIC_EQUIP).map(d=>`<div class="shopitem"><div class="line"><strong>${d.name}</strong><span class="price">50</span></div><div class="desc">新手通用裝 · ${SLOT_NAMES[d.slot]}</div><button class="smallbtn" onclick="G.player.spiritStone>=50&&(G.player.spiritStone-=50,G.inventory['${d.name}']=(G.inventory['${d.name}']||0)+1,renderAll())">購買</button></div>`).join('')}</div>`;const matShop=(G.market||[]).map((x,i)=>`<div class="shopitem"><div class="line"><strong>${x.name}</strong><span class="price">${fmt(x.price)}</span></div><div class="desc">${x.type}</div><button class="smallbtn" onclick="buyShopItem('${x.type}',${i})">購買</button></div>`).join('');const petShop=Object.values(PET_DB).map(x=>`<div class="pet ${G.pets.equipped===x.name?'equipped':''}"><div class="peticon">${x.icon}</div><div class="petname">${x.name}</div><div class="desc">${x.desc}<br>攻 ${x.atk} · 防 ${x.def} · HP ${x.hp} · 快 ${x.fast} · 暴擊 ${(x.crit*100).toFixed(1)}%</div><button class="smallbtn" style="margin-top:5px" onclick="buyPet('${x.name}')">${G.pets.owned.includes(x.name)?(G.pets.equipped===x.name?'已出戰':'出戰'):(x.cost?'購買 '+fmt(x.cost):'領取')}</button></div>`).join('');return `<div class="card"><h2 class="title">武器舖</h2><p class="desc">仿官方商鋪交易：商店只開放 Lv.1～50 武器；Lv.51 以上不販售，改由江湖打怪掉落。</p><div class="shopgrid">${weapons.map(equipCard).join('')}</div></div><div class="card"><h2 class="title">防具店</h2><p class="desc">頭盔、衣服、護腕、腰帶、鞋、項鍊、戒指、法寶、護符；商店開放至 Lv.50，50級以上裝備靠怪物掉寶取得。</p><div class="shopgrid">${armor.map(equipCard).join('')}</div></div><div class="card"><h2 class="title">Lv.50以上掉寶</h2><p class="desc">Lv.51+ 裝備不進商店名單。AI 掛機會依官方怪物資料尋找相應掉落，取得後自動比較、穿戴；3.0 怪物與裝備資料沿用官方掉落表。</p><div class="feature-list"><div><b>掉寶：</b>水鬼～靈甲鬼等官方 3.0 怪物依等級逐步取得高階裝備。</div><div><b>職業武器：</b>烈火、八步伏魔槍、絕煞、太乙維持高階取得／打造，不在 Lv.50 商店販售。</div></div></div><div class="card"><h2 class="title">新手雜貨</h2>${starter}</div><div class="card"><h2 class="title">材料商</h2><div class="shopgrid">${matShop}</div></div><div class="card"><h2 class="title">靈寵苑</h2><p class="desc">全職通用；靈寵提供被動屬性並在戰鬥中自動協戰。</p><div class="petlist">${petShop}</div></div>`}
 function enhanceSelected(slot){if(!slot||!G.equipment[slot])return toast('請先選擇已裝備部位');forgeEnhance(slot)}
-function enhanceSelectedToAvailable(slot){const it=G.equipment[slot];if(!it)return toast('請先選擇已裝備部位');let count=0;while((it.enhance||0)<15){const cost=80*((it.enhance||0)+1);if(G.player.spiritStone<cost)break;forgeEnhance(slot);count++}toast(count?`${it.name} 共強化 ${count} 次`:'靈石不足或已達 +15')}
+function enhanceSelectedToAvailable(slot){const it=G.equipment[slot];if(!it)return toast('請先選擇已裝備部位');let count=0;while((it.enhance||0)<15){const cost=30000*((it.enhance||0)+1);if(G.player.spiritStone<cost)break;forgeEnhance(slot);count++}toast(count?`${it.name} 共強化 ${count} 次`:'靈石不足或已達 +15')}
 
-function renderForge(){const enhanceTarget=window.enhanceMenuSlot||SLOT_ORDER.find(s=>G.equipment[s])||SLOT_ORDER[0];const targetIt=G.equipment[enhanceTarget];const enhanceMenu=`<div class="card"><h3 class="title">強化選單</h3><p class="desc">選擇身上裝備後直接強化；每次強化消耗 80 × 下一強化等級靈石，最高 +15。</p><div class="enhance-menu"><select onchange="enhanceMenuSlot=this.value;renderAll()">${SLOT_ORDER.map(s=>`<option value="${s}" ${enhanceTarget===s?'selected':''}>${SLOT_NAMES[s]}${G.equipment[s]?' · '+G.equipment[s].name+' +'+(G.equipment[s].enhance||0):' · 空'}</option>`).join('')}</select><button class="smallbtn hot" ${targetIt?'':'disabled'} onclick="enhanceSelected('${enhanceTarget}')">強化 +1</button><button class="smallbtn" ${targetIt?'':'disabled'} onclick="enhanceSelectedToAvailable('${enhanceTarget}')">強化至可用</button></div>${targetIt?`<p class="footer-note">目前 +${targetIt.enhance||0} → 下一級 +${(targetIt.enhance||0)+1}，需要靈石 ${fmt(80*((targetIt.enhance||0)+1))}。</p>`:'<p class="footer-note">目前沒有可強化裝備。</p>'}</div>`;const eq=SLOT_ORDER.map(s=>({slot:s,it:G.equipment[s]}));const equipHtml=eq.map(x=>`<div class="stat" style="display:block"><div class="line"><strong>${SLOT_NAMES[x.slot]}</strong><span>${x.it?x.it.name+' +'+(x.it.enhance||0):'空'}</span></div>${x.it?`<div class="desc">精煉 ${x.it.refine||0}% · 孔 ${x.it.sockets||0}/3 · 詞條 ${x.it.affix?x.it.affix.key+'+'+x.it.affix.value:'—'}${x.it.gems?.length?' · '+x.it.gems.join('、'):''}</div><div class="row" style="margin-top:5px"><button class="smallbtn" onclick="forgeEnhance('${x.slot}')">強化</button><button class="smallbtn" onclick="forgeRefine('${x.slot}')">精煉</button><button class="smallbtn" onclick="forgeSocket('${x.slot}')">開孔</button><button class="smallbtn" onclick="forgeReroll('${x.slot}')">洗煉</button></div>`:''}</div>`).join('');const target=G.equipment.weapon?'weapon':SLOT_ORDER.find(s=>G.equipment[s])||'weapon';const gemBtns=G.equipment[target]?(GEM_KEYS.map(k=>`<button class="smallbtn" onclick="forgeGem('${target}','${k}')">${k} (${G.inventory[k]||0})</button>`).join('')+OFFICIAL.blood.filter(k=>(G.inventory[k]||0)>0).map(k=>`<button class="smallbtn" onclick="forgeGem('${target}','${k}')">${k} (${G.inventory[k]||0})</button>`).join('')):'先裝備物品';const synth=EQUIP_NAMES.filter(n=>G.inventory[n]&&G.inventory[n]>=1).slice(0,24).map(n=>`<button class="smallbtn" onclick="synthesize('${n}')">3合1：${n}</button>`).join('');return enhanceMenu+`<div class="card"><h2 class="title">煉器殿 · 10部位</h2><p class="desc">八大功能完整開啟：強化、精煉、開孔、鑲嵌靈石、五行靈石、洗煉、分解、合成。</p><div class="statgrid">${equipHtml}</div></div><div class="card"><h3 class="title">靈石鑲嵌</h3><div class="row">${gemBtns}</div><p class="footer-note">每件最多 3 孔；攻擊／防禦／氣血靈石與五行靈石皆為通用材料。</p></div><div class="card"><h3 class="title">裝備合成</h3><p class="desc">選一件現有裝備作為目標，消耗任意 3 件同品質、同部位裝備。</p><div class="row">${synth||'背包沒有可合成的裝備'}</div></div>`}
+function renderForge(){const enhanceTarget=window.enhanceMenuSlot||SLOT_ORDER.find(s=>G.equipment[s])||SLOT_ORDER[0];const targetIt=G.equipment[enhanceTarget];const enhanceMenu=`<div class="card"><h3 class="title">強化選單</h3><p class="desc">選擇身上裝備後直接強化；每次強化消耗 30000 × 下一強化等級靈石，最高 +15（成功率 50%）。</p><div class="enhance-menu"><select onchange="enhanceMenuSlot=this.value;renderAll()">${SLOT_ORDER.map(s=>`<option value="${s}" ${enhanceTarget===s?'selected':''}>${SLOT_NAMES[s]}${G.equipment[s]?' · '+G.equipment[s].name+' +'+(G.equipment[s].enhance||0):' · 空'}</option>`).join('')}</select><button class="smallbtn hot" ${targetIt?'':'disabled'} onclick="enhanceSelected('${enhanceTarget}')">強化 +1</button><button class="smallbtn" ${targetIt?'':'disabled'} onclick="enhanceSelectedToAvailable('${enhanceTarget}')">強化至可用</button></div>${targetIt?`<p class="footer-note">目前 +${targetIt.enhance||0} → 下一級 +${(targetIt.enhance||0)+1}，需要靈石 ${fmt(30000*((targetIt.enhance||0)+1))}。</p>`:'<p class="footer-note">目前沒有可強化裝備。</p>'}</div>`;const eq=SLOT_ORDER.map(s=>({slot:s,it:G.equipment[s]}));const equipHtml=eq.map(x=>`<div class="stat" style="display:block"><div class="line"><strong>${SLOT_NAMES[x.slot]}</strong><span>${x.it?x.it.name+' +'+(x.it.enhance||0):'空'}</span></div>${x.it?`<div class="desc">精煉 ${x.it.refine||0}% · 孔 ${x.it.sockets||0}/3 · 詞條 ${x.it.affix?x.it.affix.key+'+'+x.it.affix.value:'—'}${x.it.gems?.length?' · '+x.it.gems.join('、'):''}</div><div class="row" style="margin-top:5px"><button class="smallbtn" onclick="forgeEnhance('${x.slot}')">強化</button><button class="smallbtn" onclick="forgeRefine('${x.slot}')">精煉</button><button class="smallbtn" onclick="forgeSocket('${x.slot}')">開孔</button><button class="smallbtn" onclick="forgeReroll('${x.slot}')">洗煉</button></div>`:''}</div>`).join('');const target=G.equipment.weapon?'weapon':SLOT_ORDER.find(s=>G.equipment[s])||'weapon';const gemBtns=G.equipment[target]?(GEM_KEYS.map(k=>`<button class="smallbtn" onclick="forgeGem('${target}','${k}')">${k} (${G.inventory[k]||0})</button>`).join('')+OFFICIAL.blood.filter(k=>(G.inventory[k]||0)>0).map(k=>`<button class="smallbtn" onclick="forgeGem('${target}','${k}')">${k} (${G.inventory[k]||0})</button>`).join('')):'先裝備物品';const synth=EQUIP_NAMES.filter(n=>G.inventory[n]&&G.inventory[n]>=1).slice(0,24).map(n=>`<button class="smallbtn" onclick="synthesize('${n}')">3合1：${n}</button>`).join('');return enhanceMenu+`<div class="card"><h2 class="title">煉器殿 · 10部位</h2><p class="desc">八大功能完整開啟：強化、精煉、開孔、鑲嵌靈石、五行靈石、洗煉、分解、合成。</p><div class="statgrid">${equipHtml}</div></div><div class="card"><h3 class="title">靈石鑲嵌</h3><div class="row">${gemBtns}</div><p class="footer-note">每件最多 3 孔；攻擊／防禦／氣血靈石與五行靈石皆為通用材料。</p></div><div class="card"><h3 class="title">裝備合成</h3><p class="desc">選一件現有裝備作為目標，消耗任意 3 件同品質、同部位裝備。</p><div class="row">${synth||'背包沒有可合成的裝備'}</div></div>`}
 
 function dungeonToday(id){const today=new Date().toISOString().slice(0,10);const st=G.dungeons[id]||{};if(st.day!==today){st.day=today;st.clears=0}G.dungeons[id]=st;return st}
 function grantDungeonItem(name,qty=1){G.inventory[name]=(G.inventory[name]||0)+qty}
@@ -652,7 +652,7 @@
 function openClassScreen(){if($('createScreen'))return;const wrap=document.createElement('div');wrap.id='createScreen';wrap.className='start-screen';wrap.innerHTML=`<div class="start-box"><div class="start-title">天下無雙・放置版</div><div class="start-sub">建立角色：依官網四大職業命名與人物屬性開始。建立後可在「角色」查看10部位裝備，裝備由玩家自行手動搭配。</div><input id="startName" class="start-input" maxlength="16" placeholder="輸入角色名稱，例如：無雙劍客" value="${G.name||'少俠'}"><div class="class-grid">${Object.entries(CLASSES).map(([k,c])=>`<div class="class-card" data-class="${k}"><div class="class-ico">${c.icon}</div><div class="class-name">${k}</div><div class="class-desc">${c.desc}</div><div class="attr-pills">${Object.entries(c.attrs).map(([a,v])=>`<span class="attr-pill">${a} ${v}</span>`).join('')}</div><div class="class-desc" style="margin-top:7px;color:var(--gold)">武器：${c.weapon} · AI：${c.ai.focus}</div></div>`).join('')}</div><div class="row start-confirm"><button id="classConfirm" class="bigbtn" disabled>選擇職業後建立角色</button></div><div class="start-note">官網職業資料：劍宗、戟門、詭流、幻道；人物屬性固定為「狠、準、穩、快、智」。初始點數：劍宗狠12／準10／穩8／快25／智5；戟門狠20／準10／穩20／快5／智5；詭流狠5／準25／穩7／快8／智15；幻道狠2／準3／穩12／快12／智31。放置版提供職業取向資訊，裝備由玩家自行手動選擇。</div></div>`;document.body.appendChild(wrap);let sel=null;wrap.querySelectorAll('.class-card').forEach(card=>card.onclick=()=>{sel=card.dataset.class;wrap.querySelectorAll('.class-card').forEach(x=>x.classList.remove('sel'));card.classList.add('sel');$('classConfirm').disabled=false;$('classConfirm').textContent=`建立角色 · ${sel}`});$('classConfirm').onclick=()=>{if(sel)chooseClass(sel)}}
 function openTeleport(){const body=$('tpBody');if(!body)return;body.innerHTML=`<div class="zone"><h4>地圖</h4>${maps.map(m=>`<div class="tp ${G.map===m.id?'here':''} ${mapUnlocked(m)?'':'lock'}"><div><b>${m.name}</b><div class="monster-meta">${m.zone} · Lv.${m.need}</div></div><button class="smallbtn" ${mapUnlocked(m)?'':'disabled'} onclick="moveMap('${m.id}');closeTeleport()">前往</button></div>`).join('')}</div>`;$('tpModal').classList.add('show')}
 function closeTeleport(){$('tpModal').classList.remove('show')}
-function updateStaticLabels(){document.title='天下無雙・放置版 '+GAME_VERSION+'（3.0副本／4槽連招）';const p=document.querySelector('header .prealm');if(p)p.textContent='Lv.1';}
+function updateStaticLabels(){document.title='天下無雙放置版';const p=document.querySelector('header .prealm');if(p)p.textContent='Lv.1';}
 /* ---------- 啟動 ---------- */
 $('tpBtn').onclick=openTeleport;$('homeBtn').onclick=returnHome;$('autoBtn').onclick=toggleAuto;$('settingsBtn').onclick=()=>goPanel('system');$('tpClose').onclick=closeTeleport;$('updateClose').onclick=closeUpdateMenu;$('inventoryDetailClose').onclick=closeInventoryDetail;$('battleClose').onclick=()=>{closeBattleScreen();renderAll()};
 window.addEventListener('beforeunload',()=>save(true));window.addEventListener('pagehide',()=>save(true));
